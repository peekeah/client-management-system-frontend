import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
        main: '#9A9EAB',
        // light: "lightblue",
        },
        secondary: {
        main: "#5D535E",
        // light: "lightgreen",
        },
        tertiary: {
        main: "#EC96A4",
        // light: "lightgreen",
        },
        accent: {
            main:"#DFE166"
        }
    },
});

export default theme;
